// Optional Firebase configuration stub
// The app operates 100% local-first using AsyncStorage.
// Firebase is completely optional and stubbed to avoid dependency issues in Snack Expo.

export const isFirebaseConfigured = false;
export const db = null;
